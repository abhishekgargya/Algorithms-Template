#include <bits/stdc++.h>
using namespace std;

// Bit-Manipulations
#define setbit(N, j) (N |= (1 << j))
#define checkbit(N, j) (N & (1 << j))
#define bitoff(N, j) (N &= ~(1 << j))
#define bittoggle(N, j) (N ^= (1 << j))
#define LSBvalue(N) (N & (-N))
#define highbits(N) ((1 << j) - 1)
#define twopower(N) (!(N & (N - 1)))

int main()
{
    // code here
    return 0;
}